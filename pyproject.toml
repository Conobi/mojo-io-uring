[project]
name = "mojo-io-uring"
version = "0.1.0"
description = "io_uring userspace interface for Mojo"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "mojox",
]

[tool.uv]
package = false
